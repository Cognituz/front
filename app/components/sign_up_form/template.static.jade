div(
  block='ctz-sign-up-form'
  layout-fill
  layout='column'
  layout-align='center center'
)
  md-card.md-no-empty-errors-spacer
    md-button.md-button--facebook(
      ng-if='$ctrl.userType === "student"'
      ng-click='$ctrl.registerWithFacebook()'
    )
      md-icon(md-svg-src='/images/icons/facebook_logo.svg')
      |  Registrarse con Facebook

    md-divider(ng-if='$ctrl.userType === "student"')

    form(ng-submit='$ctrl.registerWithCredentials()' layout='column')
      div(layout='column' layout-padding)
        md-input-container
          label Email
          input(
            type='email'
            ng-model='$ctrl.credentials.email'
            required
            autocomplete='false'
          )

        md-input-container
          label Contraseña
          input(
            type='password'
            ng-model='$ctrl.credentials.password'
            required
            autocomplete='false'
          )

        md-input-container
          label Repetir Contraseña
          input(
            type='password'
            ng-model='$ctrl.credentials.passwordConfirmation'
            required
            autocomplete='false'
          )

        span
          | Al registrarte te manifestas en acuerdo 
          br
          | con nuestros 
          a(href='/misc/tos.pdf' target='_blank') términos y condiciones

      md-button.md-raised.md-primary(
        type='submit'
        ng-disabled='$ctrl.locked'
      ) Registrarse

    md-divider

    div(layout='column' layout-align='center center' layout-padding)
      a(
        ui-sref='app.signIn'
        ng-if='$ctrl.userType === "student"'
      )
        | ¿Tenés cuenta? Ingresá acá
